#include <stdio.h>
#include "interaction-combinators.h"

/*
void printNodeState(node_t *node){
  printf("KIND = %d | P1 = %d | P2 = %d | P3 = %d",
          getKind(*node),
          getPort(*node, PORT_1),
          getPort(*node, PORT_2),
          getPort(*node, PORT_3));
}
*/

int main() {
  /*node_t myNode1, myNode2;

  printf("\n=========== INIT ===========\n");

  nodeInit(&myNode1, 1, 0, 0, 0);
  nodeInit(&myNode2, 123456, 1, 2, 3);

  printf("myNode1:\t");
  printNodeState(&myNode1);
  printf("\nmyNode2:\t");
  printNodeState(&myNode2);

  printf("\n=========== SET KIND ==========\n");

  setKind(&myNode1, 1000);
  setKind(&myNode2, 42);

  printf("myNode1:\t");
  printNodeState(&myNode1);
  printf("\nmyNode2:\t");
  printNodeState(&myNode2);

  printf("\n=========== SET PORT ==========\n");

  setPort(&myNode1, PORT_1, 10);
  setPort(&myNode1, PORT_2, 20);
  setPort(&myNode1, PORT_3, 30);
  setPort(&myNode2, PORT_1, 100);

  printf("myNode1:\t");
  printNodeState(&myNode1);
  printf("\nmyNode2:\t");
  printNodeState(&myNode2);*/

  printf("\n=========== END ==========\n");

  return 0;
}
